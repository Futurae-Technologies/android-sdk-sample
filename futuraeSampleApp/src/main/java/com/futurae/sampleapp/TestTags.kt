package com.futurae.sampleapp

object TestTags {

    object AccountsScreen {
        const val accountItem = "accountItem"
        const val accountServiceName = "accountServiceName"
        const val accountUsername = "accountUsername"
        const val accountCode = "accountCode"
        const val lockedAccountIcon = "accountLockedIcon"
        const val generateHotpMenuItem = "accountGenerateHotpMenuItem"
        const val deleteAccountMenuItem = "accountDeleteMenuItem"
        const val blankSlate = "accountsBlankSlate"
    }

    object AccountHistoryScreen {
        const val historyList = "accountHistoryList"
        const val historyItem = "accountHistoryItem"
        const val blankSlate = "accountHistoryBlankSlate"
        const val errorBlankSlate = "accountHistoryErrorBlankSlate"
    }

    object ActivationCodeScreen {
        const val activationCodeInput = "activationCodeInput"
        const val submitButton = "activationCodeSubmitButton"
    }

    object ConfigurationScreen {
        const val submitButton = "configurationSubmitButton"
        const val corruptDbButton = "configurationCorruptDbButton"
        const val corruptKeysButton = "configurationCorruptKeysButton"
    }

    object LockScreen {
        const val bioCredsTitle = "lockBioCredsTitle"
        const val alternativeAuthButton = "lockAlternativeAuthButton"
        const val pinTitle = "lockPinTitle"
        const val pinDigitCounter = "lockPinDigitCounter"
        const val pinError = "lockPinError"
        const val numPad = "lockNumPad"
        const val numPadButtonPrefix = "lockNumPadButton_"
    }

    object QRScannerScreen {
        const val qrFrame = "qrScannerFrame"
    }

    object AdaptiveSettingsScreen {
        const val settingsList = "adaptiveSettingsList"
    }

    object AdaptiveCollectionsScreen {
        const val collectionsList = "adaptiveCollectionsList"
        const val collectionItem = "adaptiveCollectionItem"
        const val clearCollectionsButton = "adaptiveClearCollectionsButton"
    }

    object AdaptiveCollectDetailsScreen {
        const val detailsText = "adaptiveCollectDetailsText"
    }

    object SDKDebugUtilScreen {
        const val settingsList = "sdkDebugUtilList"
    }

    object DebugURIUtilsScreen {
        const val uriInput = "debugUriInput"
        const val submitButton = "debugUriSubmitButton"
    }

    object IntegritySettingsScreen {
        const val integrityRow = "integrityRow"
    }

    object MoreScreen {
        const val settingsList = "moreSettingsList"
    }

    object SDKSettingsScreen {
        const val settingsList = "sdkSettingsList"
    }

    object SplashScreen {
        const val logo = "splashLogo"
    }

    object AuthenticationConfirmationScreen {
        const val approveButton = "authApproveButton"
        const val cancelButton = "authCancelButton"
        const val denyButton = "authDenyButton"
        const val detailItem = "authDetailItem"
        const val multiNumberedChallengeOption = "authMultiNumberedChallengeOption"
    }

    object FuturaeFullScreenDecisionModal {
        const val closeButton = "decisionModalCloseButton"
        const val primaryActionButton = "decisionModalPrimaryButton"
        const val secondaryActionButton = "decisionModalSecondaryButton"
    }

    object ErrorScreen {
        const val errorTitle = "errorTitle"
        const val errorMessage = "errorMessage"
        const val resetButton = "errorResetButton"
    }

    object ResultInformativeScreen {
        const val actionButton = "resultInformativeActionButton"
        const val informativeTitle = "resultInformativeTitle"
        const val informativeDescription = "resultInformativeDescription"
    }

    object SettingsRow {
        const val settingsRow = "settingsRow"
        const val settingsToggleSwitch = "settingsToggleSwitch"
    }
}
